# 隔离网络间的屏幕二维码单向传输方案

## 1. 目的与适用范围

当内网与外网之间不存在网络、共享目录、USB、剪贴板或其他数据通道，只能由外网侧读取内网屏幕时，可使用“压缩 + 分片 + 多帧二维码 + 外网自动采集”的方式传输 UTF-8 文本文件。

本方案将屏幕视为单向光学通道。内网只负责显示二维码；外网负责截图、解码、去重、排序、拼接、解压和校验。它适合命令说明、配置和日志片段等获准传出的普通文本，不适合凭据、许可证、PDK、设计数据库或其他未经授权的数据。

## 2. 为什么使用多帧二维码

- OCR 容易混淆 `0/O`、`1/l/I`、引号、反斜杠和非 ASCII 字符。
- 单张二维码容量有限，无法可靠承载数十 KB 的文本。
- 压缩后的二进制数据拆成多张二维码，可以逐帧确定性校验。
- 外网采集程序可自动识别、去重和补采，不需要把截图内容交给 Agent 逐字阅读。

哈希值或“短码”不能替代数据本身。MD5、SHA-256 只能验证结果，不能从摘要恢复原文件。

## 3. 数据流水线

```text
原始 UTF-8 文本
  -> 压缩（优先 zstd；兼容方案 gzip）
  -> 二进制到文本编码（Base64 或 Base85）
  -> 固定大小分片
  -> 为每片添加文件 ID、序号、总片数和 CRC32
  -> 生成二维码 PNG
  -> 内网屏幕循环播放
  -> 外网截图并解码
  -> 校验分片、去重和排序
  -> 拼接并解码
  -> 解压
  -> 校验最终文件大小及 SHA-256/MD5
```

## 4. 分片协议

每张二维码使用一个自描述记录，推荐格式如下：

```text
SQ1|<file_id>|<index>|<total>|<payload_crc32>|<payload>
```

字段说明：

- `SQ1`：协议版本。
- `file_id`：本次传输的短标识，建议取压缩前文件 SHA-256 的前 12 个十六进制字符。
- `index`：从 1 开始的分片序号。
- `total`：分片总数。
- `payload_crc32`：当前 payload 的 CRC32。
- `payload`：Base64 或 Base85 编码后的当前分片。

外网端必须拒绝以下数据：协议版本未知、`file_id` 不一致、序号越界、CRC32 不匹配或同一序号出现不同内容。

## 5. 二维码参数建议

- 纠错等级：默认 `M`；屏幕存在缩放、摩尔纹或远距离拍摄时使用 `Q`。
- 单片有效载荷：建议 800–1500 个 ASCII 字符，以识别稳定性优先。
- 静区：至少 4 modules，二维码四周不要贴近终端边框。
- 显示尺寸：二维码至少 700 × 700 像素；保持整数倍缩放，禁用图像平滑。
- 前景与背景：黑白高对比，避免透明、深色主题反色和水印覆盖。
- 停留时间：每帧 0.8–1.5 秒；首次实施建议 1.5 秒。
- 播放方式：按序循环，画面同时显示 `index/total` 和文件 ID，便于人工诊断。

不要依赖 QR Structured Append。它最多支持有限数量的符号，且普通扫描器兼容性不稳定。由本方案的应用层分片协议负责重组更可控。

## 6. 内网发送端流程

1. 确认源文件是获准传出的 UTF-8 常规文本。
2. 记录原文件名、字节数、SHA-256；如已有历史校验需求，可同时记录 MD5。
3. 使用 `zstd -19` 或 `gzip -9` 压缩。只有发送端和接收端都具备 zstd 时才优先使用 zstd。
4. 对压缩结果进行 Base64/Base85 编码并按固定长度分片。
5. 为每片生成协议头和 CRC32，再生成二维码 PNG。
6. 打开二维码播放器，以固定时间间隔循环显示全部帧。
7. 保持窗口尺寸、缩放和屏幕布局不变，直到外网确认全部分片收齐。

兼容性优先的压缩编码示例：

```sh
gzip -9 -c input.txt | base64 -w 0 > payload.b64
```

该命令只演示压缩编码；正式传输仍需加入分片元数据和逐片 CRC32，不应把整段 Base64 直接交给 OCR。

## 7. 外网接收端流程

1. 固定 VMware 窗口位置和二维码所在截图区域。
2. 周期性捕获二维码区域，使用 ZXing、ZBar 或 OpenCV QRCodeDetector 解码。
3. 解析记录并校验协议版本、文件 ID、序号和 CRC32。
4. 已接收的相同分片直接忽略；损坏分片等待下一轮播放重新采集。
5. 收齐 `1..total` 后按序拼接 payload。
6. Base64/Base85 解码，随后解压为原文件。
7. 校验最终文件名、字节数和 SHA-256/MD5。只有全部一致才报告成功。
8. 保留一份接收清单，但不要把二维码截图或中间 payload 提交到项目仓库。

## 8. 完整性与恢复策略

- 分片级：CRC32 用于快速检测单帧识别错误。
- 文件级：SHA-256 用于最终完整性判断；MD5 只用于兼容已有校验值。
- 丢帧恢复：循环播放所有帧，外网只补采缺失序号。
- 重复帧：按 `file_id + index` 去重。
- 会话混淆：不同文件或不同重传必须使用不同 `file_id`。
- 中断恢复：外网将已验证分片按会话保存，重新连接后继续补采。
- 进一步增强：可增加若干 XOR/喷泉码冗余帧，但实现复杂度更高；普通文本的首选方案仍是循环播放并补采缺失片。

## 9. 容量与效率估算

以约 30 KB 的重复性较高命令说明文本为例：

- gzip 后常见为数 KB 至十余 KB，实际大小以发送端结果为准。
- Base64 会在压缩结果基础上增加约三分之一体积。
- 每帧使用 800–1500 字符 payload 时，通常需要数张到十余张二维码。
- 在 1 秒/帧并循环播放的情况下，一轮通常可在几十秒内完成。

这比终端逐页截图加 OCR 更可靠，也避免把大量截图交给语言模型处理。

## 10. 验收标准

一次传输只有同时满足以下条件才算成功：

1. 外网收齐所有声明的分片序号。
2. 每个分片 CRC32 通过。
3. 拼接后的编码可无错误解码。
4. 压缩流可完整解压。
5. 输出文件字节数与内网记录一致。
6. 输出文件 SHA-256 与内网记录一致；若提供 MD5，也必须一致。
7. 输出文件以 UTF-8 严格模式重新读取成功。

任何一项失败都不得用 OCR 猜测、人工补字符或忽略校验；应重新显示并采集缺失或损坏的二维码帧。

## 11. 安全边界

- 传输前必须确认文件范围和授权，不因“只能看屏幕”而放宽数据边界。
- 不传输密码、令牌、许可证、PDK、设计数据库、用户个人数据或内部敏感路径。
- 二维码不是加密。能够看到屏幕或截图的人都可能恢复内容。
- 如需保密，应在内网使用接收方已有公钥加密后再编码；不得在屏幕上传递解密私钥。
- 外网恢复后按既定数据管理要求保存文件，并清理不再需要的二维码截图和中间分片。

## 12. 本项目当前文件的校验示例

本次待传文件：

```text
/Data/xuyang/prime_test/scripts/all_tcl_commands_help.txt
```

内网已确认：

```text
字节数：29816
编码：UTF-8 Unicode text
MD5：fd3b2ff5b10c902575332e8bbdd616f9
```

外网恢复后必须重新计算字节数和 MD5，并与以上值完全一致。该路径只用于记录本次来源，不应写入生成的 EDA 测试脚本或可移植运行配置。
